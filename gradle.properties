# The event-forwarder version
currentVersion=3.8.5-1

# Built in Gradle properties
org.gradle.parallel=true

# Base docker image tag to use for EL7, EL8, EL9 builds and pipelines
# One Product One Docker Tag
dockerBaseImageTag=3.7
