ARG ARTIFACTORY_ENV=dev
ARG ARTIFACTORY_BASE_URL
ARG ARTIFCATORY_DOCKER_URL
ARG DOCKER_IMAGE_TAG
ARG BASE_IMAGE=${ARTIFCATORY_DOCKER_URL}.${ARTIFACTORY_BASE_URL}/cb/connector_env_base:rocky9-${DOCKER_IMAGE_TAG}
FROM $BASE_IMAGE
ARG ARTIFACTORY_BASE_URL
ARG ARTIFCATORY_DOCKER_URL
ARG BASE_IMAGE

ENV ARTIFACTORY_SERVER=${ARTIFACTORY_BASE_URL}
ENV BASE_IMAGE=$BASE_IMAGE

# Set GOCache to RW
ENV GOFLAGS -modcacherw

ADD entrypoint.sh /entrypoint.sh
RUN chmod +x /entrypoint.sh
ENTRYPOINT ["/entrypoint.sh"]
